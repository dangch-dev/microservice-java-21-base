spring:
  datasource:
    url: jdbc:mysql://localhost:3306/id_mysql_database?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: mysql_root_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    defer-datasource-initialization: true
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8081

security:
  external-jwt:
    public-key-path: file:../secrets/jwt-public.pem

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost
    instance-id: ${spring.application.name}:${server.port}

logging:
  level:
    org.springframework.security: INFO

# Required declarations (But it doesn't need to have value.)
internal:
  api:
    auth-service: repo-auth
    token-path: /oauth/token
    client-id: hMLksVQKhU5WsvacK7uIAvmB
    client-secret: pbfWXn0D4MmzzBbcCwRboSbmcT92eyPx

kafka:
  topics:
    notification: notification.in-app
    file: storage.file
    mail: notification.mail